{% extends 'base.html.twig' %}

{% block body %}
    <div class="card shadow-lg m-auto">
        <div class="card-header">Renseignez les champs pour vous inscrire</div>
        <div class="card-body">
            <form method="post">
                {{ form_start(form) }}
                {{ form_row(form.email, {'label': "E-mail"}) }}
                {{ form_row(form.password, {'label': "Mot de passe"}) }}
                {{ form_row(form.firstname, {'label': "Prénom"}) }}
                {{ form_row(form.lastname, {'label': "Nom"}) }}
                {{ form_row(form.birthdate, {'label': "Date de naissance"}) }}
                {{ form_row(form.address, {'label': "Adresse"}) }}
                {{ form_widget(form) }}
                <button class="btn btn-lg btn-primary" type="submit">S'enregistrer</button>
                {{ form_end(form) }}
            </form>
        </div>
    </div>
{% endblock %}


{#
<form method="post">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-primary" type="submit">S'enregistrer</button>
    {{ form_end(form) }}
</form>
#}